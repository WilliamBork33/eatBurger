<li>
	{{burger_name}}

	<button class="devour" data-id="{{id}}" data-devour="{{devour}}">
		{{#if burger}}submit{{else}}Eat!{{/if}}
	</button>
	
	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>
